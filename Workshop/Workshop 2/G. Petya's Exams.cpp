
#include <queue>
#include <vector>
#include <iostream>

using namespace std;

struct Exam {
    int id;
    int s;
    int d;
    int c;
};


int main() {
    int n, m;
    cin >> n;
    cin >> m;

    vector<Exam> exams(m + 1);

    for (int i = 0; i < m; i++) {
        int s, d, c;
        cin >> s;
        cin >> d;
        cin >> c;

        
    }

    for (int i = 1; i <= n; i++) {
        
    }
}